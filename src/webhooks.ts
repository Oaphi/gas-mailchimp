/**
 * @summary adds a Mailchimp Webhook
 * @param {Mailchimp.Webhooks.CommonWebhookParams} options
 */
const addWebhook = ({
  settings = getSettings(),
  listId,
}: Mailchimp.Webhooks.CommonWebhookParams): boolean => {
  throw new Error("method not implemented yet, sorry");

  //TODO: inplement
};
